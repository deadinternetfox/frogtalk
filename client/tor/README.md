# FrogTalk Tor client (planned)

Minimal, **no-JavaScript** client for `.onion` nodes — HTML forms + server-rendered or plain REST, no bundled JS runtime.

**Status:** Not implemented. See [TOR_TODO.md](./TOR_TODO.md) for the build spec.

The full web app at `/app` works over Tor today but requires JavaScript. Use only if you accept pre-alpha risk.
