# ── Secrets & runtime data ─────────────────────────────────────
.env
data/
*.db
*.sqlite
*.sqlite3

# ── Python ─────────────────────────────────────────────────────
__pycache__/
*.pyc
*.pyo
*.pyd
venv/
.venv/
*.egg-info/
*.egg

# ── Node / JS ──────────────────────────────────────────────────
node_modules/
client/desktop/app/node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# ── Build outputs ──────────────────────────────────────────────
dist/
build/
*.log
deploy.tar.gz
*.tar.gz

# ── Release binaries (served at runtime, not stored in git) ────
static/*.AppImage
static/*.deb
static/*.apk
static/frogtalk-v*.apk
static/frogtalk_*.deb
static/FrogTalk-*.AppImage

# ── IDE & OS ───────────────────────────────────────────────────
.DS_Store
.vscode/
.vscode/*
.idea/
*.swp
*.swo
*~
Thumbs.db
desktop.ini
.pytest_cache/
.mypy_cache/
.ruff_cache/

# ── Imageboard runtime data ────────────────────────────────────
imageboard/board_uploads/
imageboard/board_data/
imageboard/board_previews/
static/imageboard-standalone/board_uploads/
static/imageboard-standalone/board_data/
static/imageboard-standalone/board_previews/

# ── Frogtalk runtime ───────────────────────────────────────────
static/uploads/
uvicorn.log
frogtalk.log
/logs/
*.tmp
/tmp_*

# ── Android ────────────────────────────────────────────────────
client/mobile/android/app/build/
client/mobile/android/build/
client/mobile/android/.gradle/
client/mobile/android/local.properties
client/mobile/android/signing.properties
client/mobile/android/app/google-services.json
*.apk
*.aab

# ── Build mirror (binaries published to GitHub) ────────────────
!github-build-mirror/*.apk
!github-build-mirror/*.AppImage
!github-build-mirror/*.deb
!github-build-mirror/*.exe
!github-build-mirror/*.zip

# Release binaries — keep static outputs out of git; mirror artifacts are allowed above.
static/*.AppImage
static/*.exe
secrets/

# Generated desktop release artifacts (too large for git)
static/FrogTalk-*-win-x64.zip
client/desktop/builds/*.AppImage
client/desktop/builds/*.deb
client/desktop/builds/*.exe
client/desktop/builds/*.zip

# tmp deploy / ship scripts and logs (not part of repo)
.tmp_*.sh
.tmp_*.log
scripts/deploy_nodes.sh
client/mobile/android/frogtalk.keystore
